<div class="fullscreen-modal">
    <div class="fullscreen-modal-header">
        <button type="button" class="close x-close"><i class="icon-sonarr-close-modal"></i></button>
        {{#if id}}
            <h3>Edit Mapping</h3>
        {{else}}
            <h3>Add Mapping</h3>
        {{/if}}
    </div>
    <div class="fullscreen-modal-body remotepath-mapping-modal">
        <div class="form-horizontal">
            <div>
                <p>Use this feature if you have a remotely running Download Client. Sonarr will use the information provided to translate the paths provided by the Download Client API to something Sonarr can access and import.</p>
            </div>
            <div class="form-group">
                <label class="settings-label control-label">Host</label>
                <div class="settings-control settings-control-lg">
                    <input type="text" name="host" class="form-control"/>
                    <span class="help-block">Host you specified for the remote Download Client.</span>
                </div>
            </div>

            <div class="form-group">
                <label class="settings-label control-label">Remote Path</label>
                <div class="settings-control settings-control-lg">
                    <input type="text" name="remotePath" class="form-control"/>
                    <span class="help-block">Root path to the directory that the Download Client accesses.</span>
                </div>
            </div>

            <div class="form-group">
                <label class="settings-label control-label">Local Path</label>
                <div class="settings-control settings-control-lg">
                    <input type="text" name="localPath" class="form-control x-path"/>
                    <span class="help-block">Path that Sonarr should use to access the same directory remotely.</span>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        {{#if id}}
            <button class="btn btn-danger pull-left x-delete">Delete</button>
        {{/if}}

        <button class="btn x-close">Cancel</button>

        <div class="btn-group">
            <button class="btn btn-primary x-save">Save</button>
        </div>
    </div>
</div>