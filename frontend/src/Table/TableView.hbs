<thead>
  <tr>
    {{#if selectable}}
      <th class="select-all">
        <input type="checkbox" class="select-all-checkbox">
      </th>
    {{/if}}
    {{#each headers}}
      <th data-field="{{./name}}">{{label}}</th>
    {{/each}}
  </tr>
</thead>
<tbody>
</tbody>
